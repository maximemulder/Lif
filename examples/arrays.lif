// Arrays can be instantiated using square brackets.
let array = [0, "String", true];

// Array indexing is also done using square brackets.
print(array[1]);
array[2] = 1;

// Arrays can be iterated with for loop.
for element in array {
    print(element); // Expected output: "0", "String", "1".
}

// Potential issues:
// Arrays are currently not generic, and can thus always contain any type.
